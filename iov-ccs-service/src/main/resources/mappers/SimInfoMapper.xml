<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.hwyz.iov.cloud.iov.ccs.service.infrastructure.persistence.mapper.SimInfoMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="baseResultMap" type="net.hwyz.iov.cloud.iov.ccs.service.infrastructure.persistence.po.SimInfoPo">
        <id column="id" property="id" />
        <result column="iccid" property="iccid" />
        <result column="imsi" property="imsi" />
        <result column="msisdn" property="msisdn" />
        <result column="source_mno" property="sourceMno" />
        <result column="source_type" property="sourceType" />
        <result column="source_ref" property="sourceRef" />
        <result column="sim_status" property="simStatus" />
        <result column="binding_status" property="bindingStatus" />
        <result column="realname_status" property="realnameStatus" />
        <result column="sms_status" property="smsStatus" />
        <result column="data_status" property="dataStatus" />
        <result column="voice_status" property="voiceStatus" />
        <result column="created_time" property="createdTime" />
        <result column="updated_time" property="updatedTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="baseColumnList">
        id, iccid, imsi, msisdn, source_mno, source_type, source_ref,
        sim_status, binding_status, realname_status, sms_status, data_status, voice_status,
        created_time, updated_time
    </sql>

    <!-- 插入 -->
    <insert id="insertPo" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_sim_info (
            iccid, imsi, msisdn, source_mno, source_type, source_ref,
            sim_status, binding_status, realname_status, sms_status, data_status, voice_status
        )
        VALUES (
            #{iccid}, #{imsi}, #{msisdn}, #{sourceMno}, #{sourceType}, #{sourceRef},
            #{simStatus}, #{bindingStatus}, #{realnameStatus}, #{smsStatus}, #{dataStatus}, #{voiceStatus}
        )
    </insert>

    <!-- 批量插入 -->
    <insert id="batchInsertPo" useGeneratedKeys="true" keyProperty="id" parameterType="java.util.List">
        INSERT INTO tb_sim_info (
            iccid, imsi, msisdn, source_mno, source_type, source_ref,
            sim_status, binding_status, realname_status, sms_status, data_status, voice_status
        )
        VALUES
        <foreach collection="list" item="item" index="index" separator=",">
        (
            #{item.iccid}, #{item.imsi}, #{item.msisdn}, #{item.sourceMno}, #{item.sourceType}, #{item.sourceRef},
            #{item.simStatus}, #{item.bindingStatus}, #{item.realnameStatus}, #{item.smsStatus}, #{item.dataStatus}, #{item.voiceStatus}
        )
        </foreach>
    </insert>

    <!-- 更新 -->
    <update id="updatePo" parameterType="net.hwyz.iov.cloud.iov.ccs.service.infrastructure.persistence.po.SimInfoPo">
        UPDATE tb_sim_info t SET
        <if test="imsi != null and imsi != ''">
            t.imsi = #{imsi},
        </if>
        <if test="msisdn != null and msisdn != ''">
            t.msisdn = #{msisdn},
        </if>
        <if test="sourceMno != null and sourceMno != ''">
            t.source_mno = #{sourceMno},
        </if>
        <if test="sourceType != null and sourceType != ''">
            t.source_type = #{sourceType},
        </if>
        <if test="sourceRef != null and sourceRef != ''">
            t.source_ref = #{sourceRef},
        </if>
        <if test="simStatus != null">
            t.sim_status = #{simStatus},
        </if>
        <if test="bindingStatus != null">
            t.binding_status = #{bindingStatus},
        </if>
        <if test="realnameStatus != null">
            t.realname_status = #{realnameStatus},
        </if>
        <if test="smsStatus != null">
            t.sms_status = #{smsStatus},
        </if>
        <if test="dataStatus != null">
            t.data_status = #{dataStatus},
        </if>
        <if test="voiceStatus != null">
            t.voice_status = #{voiceStatus},
        </if>
        t.updated_time = now()
        WHERE id = #{id}
    </update>

    <!-- 逻辑删除 -->
    <update id="logicalDeletePo" parameterType="Long">
        UPDATE tb_sim_info t SET
        t.updated_time = now()
        WHERE t.id = #{value}
    </update>

    <!-- 物理删除 -->
    <delete id="physicalDeletePo" parameterType="Long">
        DELETE FROM tb_sim_info t
        WHERE t.id = #{value}
    </delete>

    <!-- 根据主键获取对应数据 -->
    <select id="selectPoById" parameterType="Long" resultMap="baseResultMap">
        SELECT <include refid="baseColumnList"/>
        FROM tb_sim_info t
        WHERE t.id = #{value}
    </select>

    <!-- 根据数据对象获取对应数据列表 -->
    <select id="selectPoByExample" parameterType="net.hwyz.iov.cloud.iov.ccs.service.infrastructure.persistence.po.SimInfoPo" resultMap="baseResultMap">
        SELECT <include refid="baseColumnList"/>
        FROM tb_sim_info t
        WHERE 1=1
        <if test="id != null">
            AND t.id = #{id}
        </if>
        <if test="iccid != null and iccid != ''">
            AND t.iccid = #{iccid}
        </if>
        <if test="imsi != null and imsi != ''">
            AND t.imsi = #{imsi}
        </if>
        <if test="msisdn != null and msisdn != ''">
            AND t.msisdn = #{msisdn}
        </if>
        <if test="sourceMno != null and sourceMno != ''">
            AND t.source_mno = #{sourceMno}
        </if>
        <if test="sourceType != null and sourceType != ''">
            AND t.source_type = #{sourceType}
        </if>
        <if test="sourceRef != null and sourceRef != ''">
            AND t.source_ref = #{sourceRef}
        </if>
        <if test="simStatus != null">
            AND t.sim_status = #{simStatus}
        </if>
        <if test="bindingStatus != null">
            AND t.binding_status = #{bindingStatus}
        </if>
        <if test="realnameStatus != null">
            AND t.realname_status = #{realnameStatus}
        </if>
        ORDER BY t.id DESC
    </select>

    <!-- 根据Map对象获取对应数据列表 -->
    <select id="selectPoByMap" parameterType="java.util.Map" resultMap="baseResultMap">
        SELECT <include refid="baseColumnList"/>
        FROM tb_sim_info t
        WHERE 1=1
        <!-- 添加自定义条件 -->
        ORDER BY t.id DESC
    </select>

    <!-- 根据ICCID查询SIM信息 -->
    <select id="selectByIccid" parameterType="String" resultMap="baseResultMap">
        SELECT <include refid="baseColumnList"/>
        FROM tb_sim_info t
        WHERE t.iccid = #{iccid}
        LIMIT 1
    </select>

    <!-- 根据ICCID判断是否存在 -->
    <select id="existsByIccid" parameterType="String" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM tb_sim_info t
        WHERE t.iccid = #{iccid}
    </select>

    <!-- 条件查询列表（支持分页） -->
    <select id="selectByCondition" parameterType="java.util.Map" resultMap="baseResultMap">
        SELECT <include refid="baseColumnList"/>
        FROM tb_sim_info t
        WHERE 1=1
        <if test="iccid != null and iccid != ''">
            AND t.iccid = #{iccid}
        </if>
        <if test="imsi != null and imsi != ''">
            AND t.imsi = #{imsi}
        </if>
        <if test="msisdn != null and msisdn != ''">
            AND t.msisdn = #{msisdn}
        </if>
        <if test="sourceMno != null and sourceMno != ''">
            AND t.source_mno = #{sourceMno}
        </if>
        <if test="sourceType != null and sourceType != ''">
            AND t.source_type = #{sourceType}
        </if>
        <if test="simStatus != null">
            AND t.sim_status = #{simStatus}
        </if>
        <if test="bindingStatus != null">
            AND t.binding_status = #{bindingStatus}
        </if>
        <if test="realnameStatus != null">
            AND t.realname_status = #{realnameStatus}
        </if>
        ORDER BY t.id DESC
    </select>

    <!-- 根据Map对象统计记录数 -->
    <select id="countPoByMap" parameterType="java.util.Map" resultType="int">
        SELECT COUNT(*)
        FROM tb_sim_info t
        WHERE 1=1
        <!-- 添加自定义条件 -->
    </select>
</mapper>
