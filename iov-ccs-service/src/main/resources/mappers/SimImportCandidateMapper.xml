<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.hwyz.iov.cloud.iov.ccs.service.infrastructure.persistence.mapper.SimImportCandidateMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="baseResultMap" type="net.hwyz.iov.cloud.iov.ccs.service.infrastructure.persistence.po.SimImportCandidatePo">
        <id column="id" property="id" />
        <result column="batch_type" property="batchType" />
        <result column="batch_no" property="batchNo" />
        <result column="source_mno" property="sourceMno" />
        <result column="iccid" property="iccid" />
        <result column="imsi" property="imsi" />
        <result column="msisdn" property="msisdn" />
        <result column="parse_status" property="parseStatus" />
        <result column="store_status" property="storeStatus" />
        <result column="failure_reason" property="failureReason" />
        <result column="created_time" property="createdTime" />
        <result column="updated_time" property="updatedTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="baseColumnList">
        id, batch_type, batch_no, source_mno, iccid, imsi, msisdn,
        parse_status, store_status, failure_reason,
        created_time, updated_time
    </sql>

    <!-- 插入 -->
    <insert id="insertPo" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_sim_import_candidate (
            batch_type, batch_no, source_mno, iccid, imsi, msisdn,
            parse_status, store_status, failure_reason
        )
        VALUES (
            #{batchType}, #{batchNo}, #{sourceMno}, #{iccid}, #{imsi}, #{msisdn},
            #{parseStatus}, #{storeStatus}, #{failureReason}
        )
    </insert>

    <!-- 批量插入 -->
    <insert id="batchInsertPo" useGeneratedKeys="true" keyProperty="id" parameterType="java.util.List">
        INSERT INTO tb_sim_import_candidate (
            batch_type, batch_no, source_mno, iccid, imsi, msisdn,
            parse_status, store_status, failure_reason
        )
        VALUES
        <foreach collection="list" item="item" index="index" separator=",">
        (
            #{item.batchType}, #{item.batchNo}, #{item.sourceMno}, #{item.iccid}, #{item.imsi}, #{item.msisdn},
            #{item.parseStatus}, #{item.storeStatus}, #{item.failureReason}
        )
        </foreach>
    </insert>

    <!-- 更新 -->
    <update id="updatePo" parameterType="net.hwyz.iov.cloud.iov.ccs.service.infrastructure.persistence.po.SimImportCandidatePo">
        UPDATE tb_sim_import_candidate t SET
        <if test="parseStatus != null and parseStatus != ''">
            t.parse_status = #{parseStatus},
        </if>
        <if test="storeStatus != null and storeStatus != ''">
            t.store_status = #{storeStatus},
        </if>
        <if test="failureReason != null and failureReason != ''">
            t.failure_reason = #{failureReason},
        </if>
        t.updated_time = now()
        WHERE id = #{id}
    </update>

    <!-- 逻辑删除 -->
    <update id="logicalDeletePo" parameterType="Long">
        UPDATE tb_sim_import_candidate t SET
        t.updated_time = now()
        WHERE t.id = #{value}
    </update>

    <!-- 物理删除 -->
    <delete id="physicalDeletePo" parameterType="Long">
        DELETE FROM tb_sim_import_candidate t
        WHERE t.id = #{value}
    </delete>

    <!-- 根据主键获取对应数据 -->
    <select id="selectPoById" parameterType="Long" resultMap="baseResultMap">
        SELECT <include refid="baseColumnList"/>
        FROM tb_sim_import_candidate t
        WHERE t.id = #{value}
    </select>

    <!-- 根据数据对象获取对应数据列表 -->
    <select id="selectPoByExample" parameterType="net.hwyz.iov.cloud.iov.ccs.service.infrastructure.persistence.po.SimImportCandidatePo" resultMap="baseResultMap">
        SELECT <include refid="baseColumnList"/>
        FROM tb_sim_import_candidate t
        WHERE 1=1
        <if test="id != null">
            AND t.id = #{id}
        </if>
        <if test="batchType != null and batchType != ''">
            AND t.batch_type = #{batchType}
        </if>
        <if test="batchNo != null and batchNo != ''">
            AND t.batch_no = #{batchNo}
        </if>
        <if test="sourceMno != null and sourceMno != ''">
            AND t.source_mno = #{sourceMno}
        </if>
        <if test="iccid != null and iccid != ''">
            AND t.iccid = #{iccid}
        </if>
        <if test="parseStatus != null and parseStatus != ''">
            AND t.parse_status = #{parseStatus}
        </if>
        <if test="storeStatus != null and storeStatus != ''">
            AND t.store_status = #{storeStatus}
        </if>
        ORDER BY t.id DESC
    </select>

    <!-- 根据Map对象获取对应数据列表 -->
    <select id="selectPoByMap" parameterType="java.util.Map" resultMap="baseResultMap">
        SELECT <include refid="baseColumnList"/>
        FROM tb_sim_import_candidate t
        WHERE 1=1
        <!-- 添加自定义条件 -->
        ORDER BY t.id DESC
    </select>

    <!-- 根据批次类型、批次号和ICCID查询记录 -->
    <select id="selectByBatchAndIccid" resultMap="baseResultMap">
        SELECT <include refid="baseColumnList"/>
        FROM tb_sim_import_candidate t
        WHERE t.batch_type = #{batchType}
        AND t.batch_no = #{batchNo}
        AND t.iccid = #{iccid}
        LIMIT 1
    </select>

    <!-- 根据批次类型、批次号和ICCID判断是否存在 -->
    <select id="existsByBatchAndIccid" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM tb_sim_import_candidate t
        WHERE t.batch_type = #{batchType}
        AND t.batch_no = #{batchNo}
        AND t.iccid = #{iccid}
    </select>

    <!-- 根据批次号查询待入库的记录 -->
    <select id="selectPendingByBatchNo" resultMap="baseResultMap">
        SELECT <include refid="baseColumnList"/>
        FROM tb_sim_import_candidate t
        WHERE t.batch_no = #{batchNo}
        AND t.parse_status = 'OK'
        AND t.store_status = 'PENDING'
        ORDER BY t.id ASC
    </select>

    <!-- 更新入库状态 -->
    <update id="updateStoreStatus">
        UPDATE tb_sim_import_candidate t SET
        t.store_status = #{storeStatus},
        <if test="failureReason != null and failureReason != ''">
            t.failure_reason = #{failureReason},
        </if>
        t.updated_time = now()
        WHERE t.id = #{id}
    </update>

    <!-- 根据Map对象统计记录数 -->
    <select id="countPoByMap" parameterType="java.util.Map" resultType="int">
        SELECT COUNT(*)
        FROM tb_sim_import_candidate t
        WHERE 1=1
        <!-- 添加自定义条件 -->
    </select>
</mapper>
